"use client";

import React, { useState } from "react";
import { motion, AnimatePresence } from "framer-motion";
import { ArrowUpRight, BookOpenText, Clock3, X } from "lucide-react";

const NOTES = [
  { slug:"forecasting-as-product", tag:"ML Systems", minutes:"4 min read", title:"Forecasting is a product problem before it is a model problem.", excerpt:"A demand forecast only creates value when the data contract, error measure, and decision loop are clear.", body:["In a forecasting system, a strong model is only one layer. The application must also make its prediction horizon, confidence, and operational assumptions visible to the person using it.", "For DemandSight, that means pairing time-series output with a geographic view and validating performance with practical measures such as MAE and MAPE. The goal is not a clever model in isolation; it is a decision that becomes easier to make.", "The engineering lesson: define the downstream decision first, then choose the model, interface, and evaluation loop that support it."] },
  { slug:"realtime-ui", tag:"Frontend", minutes:"3 min read", title:"A real-time interface needs explicit states, not just sockets.", excerpt:"Collaboration features feel trustworthy when connection, permission, and conflict states are designed deliberately.", body:["Real-time product work is often introduced as a transport problem: connect a socket and send events. The hard part for the user is understanding what the system believes right now.", "In a collaborative editor, room status, access levels, presence, reconnect behaviour, and unsaved work are all interface states. Treating them as first-class design inputs prevents a responsive UI from becoming a confusing one.", "The engineering lesson: model states before motion. Animation should explain system changes, never conceal them."] },
  { slug:"shipping-portfolio", tag:"Engineering Practice", minutes:"2 min read", title:"A portfolio is a small production system, not a static page.", excerpt:"Performance, accessibility, information architecture, and honest project evidence matter as much as visual polish.", body:["A personal site is an opportunity to practise the same habits used in product work: reusable components, graceful failure states, route-level integration, and measurable user paths.", "This portfolio uses focused modes for different audiences, lazy-loaded visual experiences, and a GitHub integration with a fallback path. Those choices make the site easier to explore while keeping the experience resilient.", "The engineering lesson: let the portfolio demonstrate how you think about delivery, not only what you can make look good."] },
];

export default function EngineeringNotes({ audio }) {
  const [selected, setSelected] = useState(null);
  return <section id="notes" style={{ background:"var(--color-bg)", borderTop:"1px solid var(--border-subtle)", padding:"6rem 0" }}><div className="container"><div style={{ maxWidth:700, marginBottom:"1.8rem" }}><div className="font-sans-title" style={{ color:"var(--color-gold)", marginBottom:"0.6rem" }}>ENGINEERING NOTES</div><h2 style={{ fontSize:"clamp(2rem,4vw,3.5rem)", margin:0 }}>How I think about <span style={{ color:"var(--color-gold)" }}>building.</span></h2><p style={{ margin:"0.8rem 0 0" }}>Short technical notes on the decisions, tradeoffs, and systems behind the projects.</p></div><div style={{ display:"grid", gridTemplateColumns:"repeat(3, minmax(0,1fr))", gap:"0.8rem" }} className="notes-grid">{NOTES.map((note, index) => <motion.article key={note.slug} initial={{ opacity:0, y:12 }} whileInView={{ opacity:1, y:0 }} viewport={{ once:true }} transition={{ delay:index * 0.06 }} className="glass-card" style={{ padding:"1.2rem", display:"flex", flexDirection:"column", minHeight:270 }}><div style={{ display:"flex", justifyContent:"space-between", gap:"0.5rem", color:"var(--color-comment)", fontFamily:"var(--font-mono)", fontSize:"0.62rem" }}><span style={{ color:"var(--color-gold)" }}>{note.tag}</span><span><Clock3 size={11} style={{ verticalAlign:"middle" }}/> {note.minutes}</span></div><h3 style={{ fontSize:"1.22rem", lineHeight:1.2, margin:"1rem 0 0.65rem" }}>{note.title}</h3><p style={{ fontSize:"0.82rem", lineHeight:1.6, margin:0, color:"var(--color-cream-muted)", flex:1 }}>{note.excerpt}</p><button onClick={() => { setSelected(note); audio?.playClick(); }} onMouseEnter={() => audio?.playHover()} style={{ alignSelf:"start", display:"flex", alignItems:"center", gap:"0.35rem", marginTop:"1rem", border:0, padding:0, background:"none", color:"var(--color-cream)", cursor:"pointer", fontFamily:"var(--font-mono)", fontSize:"0.68rem" }}>Read note <ArrowUpRight size={13}/></button></motion.article>)}</div></div><AnimatePresence>{selected && <motion.div initial={{ opacity:0 }} animate={{ opacity:1 }} exit={{ opacity:0 }} onClick={() => setSelected(null)} style={{ position:"fixed", inset:0, zIndex:1100, background:"rgba(0,0,0,0.72)", display:"grid", placeItems:"center", padding:"1rem" }}><motion.article initial={{ y:18, scale:0.98 }} animate={{ y:0, scale:1 }} exit={{ y:18, scale:0.98 }} onClick={(event) => event.stopPropagation()} className="glass-card" style={{ width:"min(680px, 100%)", maxHeight:"85vh", overflow:"auto", padding:"1.5rem", background:"var(--color-bg-card)" }}><div style={{ display:"flex", justifyContent:"space-between", gap:"1rem", alignItems:"start" }}><div><div className="font-sans-title" style={{ color:"var(--color-gold)" }}><BookOpenText size={13} style={{ verticalAlign:"middle" }}/> {selected.tag} / {selected.minutes}</div><h2 style={{ margin:"0.65rem 0 1rem", fontSize:"clamp(1.65rem,4vw,2.3rem)" }}>{selected.title}</h2></div><button onClick={() => setSelected(null)} aria-label="Close note" style={{ border:0, background:"none", color:"var(--color-comment)", cursor:"pointer" }}><X size={18}/></button></div>{selected.body.map((paragraph) => <p key={paragraph} style={{ fontSize:"0.95rem", lineHeight:1.8, margin:"0 0 1rem" }}>{paragraph}</p>)}</motion.article></motion.div>}</AnimatePresence></section>;
}